name: Prepare Node.js environment
description: Install Node.js 22 with npm cache, then install dependencies with npm ci

runs:
  using: composite
  steps:
    - name: Install Node.js
      uses: actions/setup-node@v5
      with:
        node-version: 22
        cache: npm

    - name: Install dependencies
      run: npm ci
      shell: bash
